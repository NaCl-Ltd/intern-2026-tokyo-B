**① ビュー修正（準備運動）**

- テキストを変える
    
    ```
    ## 概要
    画面上部（ヘッダ部）に表示されているアプリ名を変更する
    
    ## 仕様詳細
    - SAMPLE APPではなく、アプリの名前を考えてそのアプリ名が表示されるように変更する
    
    <img width="795" height="173" alt="Image" src="https://github.com/user-attachments/assets/1748d844-5b90-446f-87a9-6d0b4bb92c48" />
    ```
    
- 色を変更する
    
    ```
    ## 概要
    画面上の指定箇所の背景色を変更する
    
    ## 仕様詳細
    - 以下の赤い四角で囲った箇所の背景色を「#EEEEEE」に変更する（他に良さそうな色があれば提案してもよい）
    
    <img width="698" height="641" alt="Image" src="https://github.com/user-attachments/assets/5a8e9518-3c65-4f52-a3cd-cb94b9e525b8" />
    ```
    
- アイコン画像を変更する
    
    ```
    ## 概要
    アプリのタイトル横に表示されているアイコン画像を変更する
    
    ## 仕様詳細
    - 現在表示されている虹色の水晶のアイコンを、指定の新しいアイコン画像（Rubyのロゴ）に変更する
    - アイコンのサイズは現状と同じままとする
    
    [新しいアイコン画像](https://github.com/NaCl-Ltd/intern-2023-A/assets/1905688/d79dc9f8-886e-4d1f-a2cf-eefe4d3fb8e2)

    アイコンのサイズは今と同じままでお願いします。

    （画像取得先： https://www.ruby-lang.org/ja/about/logo/ ）
    ```
    
- フッターの表記・リンク変更
    
    ```
    ## 概要
    画面下部（フッター）の表記・リンクを変更する

    ## 仕様詳細
    - 現状のフッターに表示されているリンク（Contact, News等）の文言、またはリンク先を変更する
    - 具体的な変更内容は自由に決めてよい（例: 会社名の表記変更、リンク先URLの変更、SNSリンクの追加など）
    
    注意: Aboutはこれ以降の課題対象の為変更しないでください.
    
    <img width="877" height="58" alt="Image" src="https://github.com/user-attachments/assets/c51d8a26-10a2-4ac3-9767-e4369bb49bc3" />
    ```
    

**② ビュー修正2週目**

- アプリの表示言語を日本語化する
    
    ```
    ## 概要
    アプリの表示言語を英語から日本語に変更する（多言語対応はせず日本語化のみでよい）
    
    ## 仕様詳細
    - 既存の英語表示を日本語に書き換える（多言語対応は不要）
    - 全画面を日本語化する必要はない（画面下部メニューの「About」「Contact」等は英語のままでよい）
    - 以下のよく使う画面を優先して日本語化する
      - ログイン画面（「Welcome to the Sample App」のメッセージ、「Sign Up Now!」ボタン等）
      - ホーム画面（following/followers、view my profile 等）
    ```
    
- アップロード画像の最大表示幅を変更する
    
    ```
    ## 概要
    マイクロポストに添付した画像の最大表示サイズを変更する
    
    ## 仕様詳細
    - 現状500x500で表示される画像サイズを、300x300ないし400x400、ベストと思うサイズに調整する
    ```
    
- 設定ファイルに書かれたテキストを表示する
    
    ```
    ## 概要
    Aboutページに表示される、アプリ改善を行うグループメンバー名を設定ファイルから表示できるようにする
    
    ## 仕様詳細
    - Aboutページ（<http://localhost:3000/about）には会社名（ネットワーク応用通信研究所）が設定ファイルの内容として表示されている>
    - 同様に、設定ファイルにこのアプリの改善を行うグループメンバーの名前（本名でなくGitHubアカウント名などで可）を記載する
    - その設定ファイルの内容からメンバー名を表示するよう改修する
    ```
    
- エラーページ（404/500）のカスタマイズ
    
    ```
    ## 概要
    404（Not Found）・500（Internal Server Error）のエラーページを、Rails標準のデフォルトページから独自デザインに変更する

    ## 仕様詳細
    - public/404.html, public/500.html の内容を、アプリのデザインに合わせたページに変更する
    - エラーページからトップページへ戻るリンクを設置する
    ```
    

**③ モデルにカラム追加**

- プロフィールに自己紹介文を追加
    
    ```
    ## 概要
    プロフィールに自己紹介文を登録・表示できるようにする
    
    ## 仕様詳細
    - メニューの「Account」>「Settings」から開けるプロフィール更新画面で自己紹介文を編集できるようにする
    - 登録した自己紹介文はユーザ一覧、およびユーザ詳細画面に表示する
    ```
    
- プロフィールに出身地を追加
    
    ```
    ## 概要
    プロフィールに出身地を追加する
    
    ## 仕様詳細
    - プロフィール更新画面に出身地の入力欄を追加する
    - ユーザ一覧画面にて、出身地で絞り込む欄を追加する
    
    ### 自由入力
    - 名前やメールアドレス同様、自由に入力できる形式
    - ユーザ検索欄にもフリーなキーワード入力欄を追加、部分一致で絞り込みを行う
    ```
    
- ニックネームを追加する
（元issueにあった「マイクロポスト本文中のニックネームをリンク化する」仕様は、③カテゴリの範囲（カラム追加＋表示）を超えるため今回は対象外とする）
    
    ```
    ## 概要
    ユーザにニックネーム（そのユーザを一意に特定する名前）を追加する
    
    ## 仕様詳細
    - プロフィール更新画面からニックネームを登録できるようにする
      - X（旧Twitter）におけるユーザID（`@hogehoge`）のようなもの
      - 他のユーザと被るニックネームは登録不可
    - 登録したニックネームは各画面のユーザプロフィール表示欄に表示する
    ```
    
- プロフィールにWebサイトURL追加
    
    ```
    ## 概要
    プロフィールにWebサイト（個人ブログ、SNS等）のURLを追加する

    ## 仕様詳細
    - プロフィール更新画面にWebサイトURLの入力欄を追加する
    - 登録したURLはユーザ詳細画面にリンクとして表示する
    - 未入力の場合は何も表示しない
    ```
    

**④ クエリ・検索（scope/where）**

- ユーザの検索機能
    
    ```
    ## 概要
    ユーザ一覧に検索機能を追加（ユーザ名またはメールアドレスの部分一致）
    
    ## 仕様詳細
    - 現状ではユーザ一覧画面ではすべてのユーザが表示されるのみ
      - 全部チェックして目的のユーザを探さなければならない（いまどきそんなシステムないよ！）
    - ユーザ一覧画面にキーワード入力欄と「検索」ボタンを設置する
      - 「検索」ボタンを押すと、入力したキーワードに一致するユーザ名またはメールアドレスを持つユーザのみが一覧表示される
    - 検索条件に合致するユーザが1人もいない場合はその旨を表示する
    ```
    
- マイクロポストのキーワード検索機能
    
    ```
    ## 概要
    マイクロポスト一覧に検索機能を追加（投稿本文の部分一致）
    
    ## 仕様詳細
    - マイクロポスト一覧画面にキーワード入力欄と「検索」ボタンを設置する
    - 「検索」ボタンを押すと、入力したキーワードを本文に含むマイクロポストのみが一覧表示される
    - 検索結果は既存のページネーションと組み合わせて表示する
    - 検索条件に合致するマイクロポストが1件もない場合はその旨を表示する
    ```
    
- 最近投稿されたポストを表示する機能
    
    ```
    ## 概要
    フォローしている人たちの最近投稿されたマイクロポストを表示する機能

    ## 仕様詳細
    - 新たに「New」メニューを追加する
    - 「New」メニューの遷移先ではフォローしている人たちの最近投稿されたマイクロポストのみを表示する
    - 最近とは48時間以内とし、最大10件のみ表示する

    ### Advanced
    - 「48時間以内」「最大10件」という条件を設定ファイルから可変にすると便利そう
    - 「New」メニューが、最近のマイクロポストが何件あるかわかる表記だと便利そう（8件だったら「New(8)」になるとか）
    ```
    
- 投稿数の多いユーザランキング
    
    ```
    ## 概要
    マイクロポストの投稿数が多いユーザのランキングを表示する

    ## 仕様詳細
    - 新たにランキング画面（または既存画面内のセクション）を追加する
    - 投稿数の多い順にユーザを一覧表示する（上位10名程度）
    - 各ユーザの投稿数も併せて表示する
    ```
    

**⑤ 新規モデル＋コントローラー追加**

- Like機能
    
    ```
    ## 概要
    他人のマイクロポストをLike（いいね）できる機能
    
    ## 仕様詳細
    - 他人のマイクロポストをLikeする機能を追加する
      - Likeするとマイクロポストにマークが表示される
      - Likeの取り消しも可能
    - 自分がLikeしたマイクロポストを一覧表示できる
    - あるマイクロポストをLikeしたユーザを一覧表示できる
    - マイクロポストにLike数を表示する
    
    ### Advanced（時間があれば）
    - Likeを発展させてユーザ体験を向上させる機能を考え、実装してみよう
    ```
    
- お気に入り（ブックマーク）機能
    
    ```
    ## 概要
    他人のマイクロポストをお気に入り（ブックマーク）登録できる機能
    
    ## 仕様詳細
    - 他人のマイクロポストをお気に入り登録・解除できる
    - 自分がお気に入り登録したマイクロポストを一覧表示できる
    - Bookmarkモデル（User × Micropostの中間テーブル）を新設する
    - フォロー機能（Relationshipモデル）の実装パターンを参考にできる
    ```
    
- マイクロポストへのコメント機能
    
    ```
    ## 概要
    マイクロポストに対してコメントを投稿できる機能
    
    ## 仕様詳細
    - マイクロポスト詳細（または一覧の各投稿）にコメント投稿フォームを追加する
    - コメントは投稿者・本文・投稿日時を表示する
    - Commentモデル（belongs_to :micropost, :user）とCommentsControllerを新設する
    - 自分のコメントは削除できる
    ```
    
- リポスト機能
    
    ```
    ## 概要
    他人のマイクロポストを自分のフィードに再投稿（リポスト）できる機能

    ## 仕様詳細
    - 他人のマイクロポストに「リポスト」ボタンを追加する
    - リポストすると自分のフィードにも該当マイクロポストが表示される（誰がリポストしたかが分かるように表示する）
    - リポストの取り消しも可能にする
    - Repostモデル（belongs_to :user, :micropost）を新設する
    ```
    

**⑥ 既存コントローラーの改修**

- マイクロポストの編集機能
    
    ```
    ## 概要
    投稿済みのマイクロポストの本文を編集できる機能
    
    ## 仕様詳細
    - 自身が投稿したマイクロポストにのみ「編集」リンクを表示する（削除リンクの近く）
    - MicropostsControllerにedit/updateアクションを追加する
    - 所有者チェックは既存の削除機能（correct_user）の実装を流用する
    ```
    
- 管理者機能：他人のマイクロポストを削除できる
    
    ```
    ## 概要
    管理者ユーザでログインしている場合は他人のマイクロポストを削除可能とする
    
    ## 仕様詳細
    - 現状では自身の投稿したマイクロポストのみが削除可能である
    - 管理者ユーザでログインしている場合は、他の全ユーザのマイクロポストにも削除リンクが表示されるようにする
    - 「マイクロポストの削除後に復活できるようにする」（⑧チャレンジ／既存#14）が対応済みの場合、管理者ユーザは他のユーザの削除マイクロポスト一覧画面も表示可能とする
    - 管理者ユーザの場合は他のユーザの削除マイクロポストも削除取消できるものとする
    ```
    
- 固定マイクロポスト機能
    
    ```
    ## 概要
    指定したマイクロポストをユーザのフィード画面の一番上に常に表示させる機能
    
    ## 仕様詳細
    - 自身が投稿したマイクロポストにのみ、「固定する」的なメニューが表示される（削除メニューの横あたりに表示）
    - 「固定」指定ができるマイクロポストは1ユーザにつき1つのみ
    - 固定マイクロポストが既にある状態（これをAとする）で、別のマイクロポスト（これをBとする）の「固定する」メニューを選択した場合、Aは固定ではなくなり、Bが新たな固定マイクロポストとなる
    - 以下の画面では固定マイクロポストを一番上に常に表示させる
      - フィード画面（ホーム画面）：自身のマイクロポストとフォローしているユーザのマイクロポストが時系列で表示されている中、自身の固定マイクロポストは常に一番上に表示する
      - ユーザ詳細画面：特定のユーザ（自分でなくてもいい）の詳細画面ではそのユーザのマイクロポストのみが時系列で表示されるが、固定マイクロポストが存在する場合はそれを一番上に表示する
    
    ### Advanced
    - 別の固定マイクロポストを指定しなくても、固定解除できるようにする
    - 既に「固定」指定しているマイクロポストの場合は「固定解除」的なメニューにする
    ```
    
- マイクロポストの詳細ページ
    
    ```
    ## 概要
    マイクロポスト単体の詳細ページを追加する

    ## 仕様詳細
    - MicropostsControllerにshowアクションを追加する
    - マイクロポスト一覧の各投稿から詳細ページへ遷移できるようにする
    - 詳細ページには投稿者・本文・投稿日時・添付画像を表示する
    ```
    

**⑦ gem追加**

- アプリの多言語化
    
    ```
    ## 概要
    Rails国際化（i18n）の機能を使って、アプリを多言語化（英語・日本語のみ）する。使用gemは rails-i18n に統一する
    
    ## 仕様詳細
    - rails-i18n gemを使用する（日本語の複数形化・日付/時刻フォーマット等のロケールデータが揃っているため）
    - 表示言語の切り替え方法は <https://railsguides.jp/i18n.html> を参照する
    - 以下のようにURLの末尾にlocale指定を付加することで表示言語を切り替えられるようにすると動作確認しやすい
      - <http://localhost:3000?locale=en>
      - <http://localhost:3000?locale=ja>
    ```
    
- マイクロポストのページネーション
    
    ```
    ## 概要
    現状 will_paginate gem で実装されているマイクロポストのページネーションを、kaminari gemを使った実装に置き換える
    
    ## 仕様詳細
    - 表示件数・「もっと見る」リンクなど、現状のページネーション挙動（デフォルトで最新10件表示、以降はページ送りで表示）は変えない
    - will_paginate / bootstrap-will_paginate をGemfileから外し、kaminariに置き換える
    - ビュー側のページネーションリンク表示もkaminariのヘルパーに置き換える
    ```
    
- タグ機能
    
    ```
    ## 概要
    acts-as-taggable-on gemを使って、マイクロポストにタグを付けられるようにする
    
    ## 仕様詳細
    - マイクロポスト投稿時にタグ（複数可）を入力できるようにする
    - マイクロポストの表示欄に付与されたタグを表示する
    - タグ名をクリックすると、そのタグが付いたマイクロポストのみの一覧画面に遷移する
    ```
    
- 投稿のMarkdown対応
    
    ```
    ## 概要
    redcarpet gemを使って、マイクロポストの本文をMarkdown記法で装飾できるようにする

    ## 仕様詳細
    - redcarpet gemを導入する
    - マイクロポスト本文にMarkdown記法（強調、リンク、リスト等）を使った場合、表示時にHTMLへ変換して装飾表示する
    - XSS対策のため、必要なサニタイズ処理も行う
    ```
    

**⑧ チャレンジ（任意）**

- マイクロポストの削除後に復活できるようにする
    
    ```
    ## 概要
    削除したマイクロポストを削除取消できるようにする
    
    **キーワード: 論理削除**
    
    ## 仕様詳細
    - 「削除」を選択したマイクロポストをDBから本当に消してしまうのではなく、通常の画面には表示されない状態ということにする
    - 削除したマイクロポストの一覧を表示する画面を追加する
      - ここでは自らが削除したマイクロポストのみを表示できるものとする（他のユーザのマイクロポストは表示しない）
      - マイクロポスト一つ一つに「削除取消」のリンクを付与する
      - 「削除取消」したマイクロポストは再び通常のマイクロポスト一覧に表示可能になる
    ```
    
- ひとつの投稿に複数の画像をアップロード可能にする
    
    ```
    ## 概要
    マイクロポストに複数の画像を添付して投稿できるようにする
    
    ## 仕様詳細
    - 現状のファイル選択ダイアログで複数のファイルを選択できるようにする
    - 添付可能な画像の枚数は最大で4とする
    - マイクロポスト表示欄には添付されたすべての画像を表示する
    
    ### Advanced
    - 添付された画像の枚数に応じて表示される画像のサイズが変わるとGood
    - サムネイル表示 => クリックすると大きなサイズで表示、などができるとよりGood
    ```
    
- URLのリンク化とフォロー数表示
統合後、既存#15・#17はクローズし、この新規issueへの誘導コメントを残す。
    
    ```
    ## 概要
    （1）マイクロポスト本文中にURLが含まれる場合はリンクにする
    （2）フォロー/フォロワー一覧に各ユーザのフォロー数・されてる数を表示する
    
    ## 仕様詳細
    (1)
    - マイクロポスト本文に「http://」または「https://」から始まる文章が存在する場合はリンク化する
    (2)
    - 自身のプロフィールにあるフォロー数/フォロワー数のリンクをクリックすると、つながりのあるユーザの一覧が表示される
    - この一覧の情報として、各ユーザのフォロー数、フォロワー数を表示する
    ```
    
- 通知機能（フォロー・いいね時）
    
    ```
    ## 概要
    フォローされた時・自分の投稿にいいね（Like）された時に通知を受け取れる機能

    ## 仕様詳細
    - Notificationモデルを新設し、フォロー・Like発生時に通知を作成する
    - 通知一覧画面を追加し、未読/既読を管理する
    - ヘッダー等に未読通知数を表示する

    ### Advanced
    - Action CableやTurbo Streamsを使ってリアルタイムに通知を反映できるとよりGood
    ```
