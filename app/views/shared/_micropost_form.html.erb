<%= form_with(model: @micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t('shared.label.suggest_micropost') %>
  </div>
  <%= f.submit t('shared.label.micropost'), class: "btn btn-primary" %>
  <span class="image">
    <%= f.file_field :image, accept: "image/jpeg,image/gif,image/png" %>
  </span>
  <div class="field" style="margin-bottom: 10px;">
    <%= f.text_field :tag_list, value: f.object.tag_list.join(','), class: "form-control", placeholder: "タグをカンマ区切りで入力 (例: ruby, rails)" %>
  </div>
<% end %>
