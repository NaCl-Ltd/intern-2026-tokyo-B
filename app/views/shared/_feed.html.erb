<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :static_pages, action: :home } %>
<% else %>
  <section class="nothing_microposts">
    Nothing Microposts
  </section>
<% end %>
